{
  "name": "api",
  "scripts": {
    "prepare:db": "prisma migrate deploy && prisma generate --accelerate",
    "dev": "pnpm run prepare:db && wrangler dev src/index.ts",
    "deploy": "pnpm run prepare:db && wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.9.1",
    "@prisma/extension-accelerate": "^0.6.3",
    "bcryptjs": "^2.4.3",
    "hono": "^4.0.4",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "@types/bcryptjs": "^2.4.6",
    "prisma": "^5.9.1",
    "wrangler": "^3.25.0"
  }
}
